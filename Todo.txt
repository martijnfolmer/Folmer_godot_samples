
27 april - 3 mei
-- debris : come to complete stop when below threshold (done)
-- wall/glass : replace particles with debris (done)
-- input : make all spacebar and keys which we have hardcoded, and use input instead (done)
-- sfx : pass on volume (done)
-- sfx : max number of same single sfx (done)
-- goblin all : reduce in speed quicker and slam less quickly (done)
-- Astar : figure out how to reduce goblin (astar needs to be larger masks of object, goblins smaller) (done)
-- respawn (reset lvl using test code) (done, in player script)
-- camera : start at the player locaiton on start up (done)
-- astar path : check if first point or next point is closer to current when we recalculate (done)
-- astar grid visualisation

# Astar to do:
# TODO: inside of this script
	# TopLeft and BottomRight for astar grid, which we can find, and use for finding topLeft and Bottom
	# If no cell, it is blocked as well
	# forward propagation : turn grid values to -1, calling position to 0
	# backward propagation : find path, and store
	# check if first element is better second element, otherwise, remove first element
	# Line of sight : if we can have line of sight, and blocking elements also don't block, we use that as path

	# TODO in chase_and_melee
	# In chase_and_melee -> we do all the astar calling, and every N frames we recalculate, taking state into account
	# In chase_and_melee -> follow the path and move with it.

	# Upgrades: get a cell grid, which should only be updated if the cells change? If that is even something we do
	# upgrades: Movement is not strictly along a line, but rather it has movement speed and rotation speed



NICES
-- create a lvl that we load
-- create another lvl 
-- move between two lvls


Todo:
	Walls and glass:
		- debris is actually pieces of the wall, and kicking it just makes it spike
	Astar :
		- make it so we don't get stuck on walls, when next to walls (because if enemy hugs walls, they are stuck)
		- give goblins different speeds based on distance to player? so those behind lag behind
		- Where is the grid made? Because we don't want a path that gets out of bounds
	floor :
		- 
	goblin : 
		- dazed sprite replace (with something with an outline)
		- sprite outline crazy outline shuddering
	player :
		- replace sprite with some red thing
	camera:
		
	walls:
		- indestructible walls
	debris:
		- create script that will make them move again.
	input:
		
	SFX :
		
	Textbox :
		- loud and soft text should have loud and soft sfx sound
		- colored shadow for the txt
		- different txt sounds when murdering
	General:
		- create 2 lvls
		- sounds for txt ticks, different for alchemist or other one
		- talk to npc in screen, spawn stuff
		- knockback code cleanup
	Goblin all:
		- don't just freeze when we are done 
		- arms go back and forth when walking
		- on componcelloverlap, add a list of sprites instead of just one, and rotate them all
	Ranged goblin:
		- gets in range, wind up (flashing), fire an arrow
		- arrow is a sepparate thing, and can be kicked at the right time
		- hold a colored bow in front of them
	Shield goblin:
		- gets in range, but must be kicked again when dazed
		- has a similiar attack to goblin basic
	goblin brute:
		- can move through walls and glass
	goblin flier:
		- can fly over pits, and you need to kick something into them
	Kick goblin:
		- big kickback to the player
		- getting dazed
		- if kick connects with kick, it blows other things away
	spear goblin:
		- throws a spear that keeps you in place, and you can kick the spear away
	final boss:
		- heart of the dungeon, must kick enough goblins into it.
		- floating off the platform, you must kick goblins onto it
		- it messes with the platform, changing the arena to previous lvls
	player:
		- when damaged, we bleed
	

	Screen
	
	Knight and alchemist:
		- replace text box with character portraits (WIP)
		- put an alchemist character there we can interact with, initializing the text box 
		- put some dialogue in a csv, then to a json, and read that json from here
		
	
	What happens when you hurt the alchemist?
	-- they dissapear, no more healing potions
	-- so you don't heal inbetween chambers
	
	- global pause
	-- pause player and goblins and debris when global pause is active (can be an enum and auto load)
	
	
	Options menu
		- different resolutions
		- global screen shake
		- sfx slider
		- music slider
		- colorblind mode
		- deathless mode
		- rebindable keys
		- fullscreen and windowed



Concrete steps (20 - 26 april):
	- create a debris.tscn, with a movement, and slowdown, and collision, and falling down (done)
	- move arm of goblin back when we are recharging after attack (done)
	- goblin a star, make a list export of groups that block, add glass (done these)
		- has_los doesn't work
		- pillars are not blocking
		- neither are glass and wall
	- cleanup text box.gd code (done)
	- function to create txt box and pass list (done)
	- text_box sfx every thing (done)
	- all enums in a global script (start with goblin enums)	(done)
	- different pitch in the sfx singleton (done)
	
MAYBES:
	- goblin all : if mostly off off edge, move a little more towards the edge.
	- goblin all : auto aim at pillars or other goblins when kicked. (maybe)
